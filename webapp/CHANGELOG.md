# 更新日志 · Changelog

本项目（个股看板 Stock Dashboard）遵循[语义化版本](https://semver.org/lang/zh-CN/)。

格式：`主版本.次版本.修订号`
- **主版本**：不兼容的重大改动（数据结构/部署方式变化等）
- **次版本**：向后兼容的新功能
- **修订号**：向后兼容的问题修复

Git 标签以 `webapp-v<版本>` 命名（避免与仓库内 skills 的 `v*` 发布标签冲突）。

---

## [1.1.0] - 2026-07-01

### 新增:止损纪律硬化(plan #1)
- 持仓页顶部**红 banner**:有「遗漏」(未确认)的无止损持仓时醒目提示「组合风险未封顶」,点代码定位并高亮该行
- 新建持仓**止损必填**;确需无止损须**二次确认打标**(`no_stop_ack`),加仓合并沿用既有止损不拦
- 新增两个纪律阈值:**单笔最大风险%**(默认 1.5)、**组合最大热度%**(默认 6);计算器 / 持仓行 / 热度卡超阈值标红
- 组合风险敞口卡:**无止损成本敞口单列一行**(不再用会误导的单一数字);持仓行区分「未设止损」(红)与「无止损·已确认」(灰)

### 新增:结构化交易日志 + 绩效面板(plan #2 A+C)
- 新增「🎯 绩效面板」页:胜率、平均盈利/亏损%、**盈亏比**、**期望值**、最大回撤、盈/亏单平均持有天数、纪律达成率,并**按 setup 分组**统计
- **交易日志弹窗**:setup 类型、入场理由、信念度/情绪(1–5)、退出理由、错误标签(多选)、教训;持仓行与绩效逐笔明细均可打开(已填标 ✍)
- 新表 `journal`(一持仓一日志);绩效由 `positions`/`trades`/`journal` 实时聚合,不额外落库

### 数据模型
- `positions` 新增 `no_stop_ack`(无止损确认标记);新增 `journal` 表(均含幂等迁移)

### 技术
- `_portfolio_state` 汇总新增 `noStopCount / noStopCost / noStopCostPct`,每仓位新增 `riskPct`
- 新增 `GET /api/journal`、`GET/POST /api/journal/<pid>`、`GET /api/performance`

---

## [1.0.1] - 2026-07-01

### 新增:资金曲线 · 收益跟踪
- 新增「资金曲线」页,跟踪账户总资金量与收益率随时间变化
- **每日快照(8点→8点窗口)**:美东 20:00 自动锁定当日总资金量 + 收盘持仓市值/成本;持仓页更新总资金量时即时落当日快照
- 支持**手动补录 / 编辑 / 删除**历史快照;区间切换(全部 / 近30 / 近7天)
- 收益口径:累计/区间收益率按每日收益率连乘的**时间加权**计算,**净入金(出入金)不计入收益**;当日收益 = 今值 − 昨值 − 当日净入金
- **echarts 双轴曲线**:总资金量 + 累计收益率,出入金标记点

### 技术
- 独立 `/api/equity` 路由与 `equity_snapshots` 表,与「每日复盘」(`/api/snapshots`)命名空间分离,互不冲突

---

## [1.0.0] - 2026-07-01

首个正式版本。Flask + yfinance 的个人炒股决策仪表盘，四大页面 SPA，数据落盘 SQLite，经 Cloudflare Tunnel + Access 部署在 ECS。

### 个股看板
- 一键决策卡（SEPA + 估值 + 期权墙 → 买入/观察/回避 + 建议仓位）
- K 线（4h / 1d / 1w）+ MA5/10/20/50/200 + 成交量 + 可选期权墙位叠加
- SEPA 趋势模板评分、财报日/业绩、流动性评分、关键财务、分析师评级、新闻流
- 价格 / 跌破 20MA / 触及止损 实时预警（后端评估，触发顶部 banner，可经 Telegram 推送）
- 估值三角定位（DCF + 分析师目标 + 远期 PE）、分析师预期与 EPS 修正
- 期权墙：Max Pain、Call/Put OI 支撑压力墙、净 GEX + Gamma Flip、Put/Call 比率
- 多股对比：归一化走势、区间涨幅/波动/Beta、相关性矩阵
- 个股研究笔记（按标的归档）

### 仓位计算器
- 按买入价/止损/总资产 + 仓位上限 + 风险上限算最大可买股数
- **可手改买入股数**（默认最大值），派生金额实时重算；改任一条件即重置回最大值
- 一键「记入持仓」，按实际填入的股数记录

### 持仓 / 跟踪
- 总资金量持久化，作为唯一真相；显示已投入/可用现金/浮盈亏/**仓位占账户（按市值）**/组合风险敞口
- 持仓表：实时浮盈亏、**距止损（回落跌幅，绿赚红亏）**、**风险敞口（$）**、备注
- 行内编辑（股数/成本/现价/止损/目标/备注）；编辑单笔买入的仓位会同步修正其交易明细
- **加仓自动合并**到同标的仓位（加权平均成本）
- **手填现价**支持无实时行情的标的（期权 / 港股等）
- **空头支持**（负股数，如卖出开仓/备兑期权）：盈亏方向、平仓（全平/部分）均正确
- **平仓**：行内表单输入平仓价 + 数量，支持部分平仓（保留剩余仓位并注明）与清仓（记录累计已实现盈亏）

### 交易明细（买卖流水台账）
- 每笔买入/卖出独立记录，带精确时间戳，**严格按成交先后倒序**（最新在最上面）
- 分页显示，每页 20 条；可单条删除
- 已实现盈亏逐笔记录，清仓时汇总该标的总盈利

### 每日复盘
- **「今天」实时显示**当前组合（改总资金量/价格/交易即时反映）
- 每个交易日 **17:00（美东）自动冻结**当日存档（持仓、交易、盈亏、敞口），历史日期为只读快照
- 每日可写复盘文字并持久化，供日后回查
- 「立即冻结/更新今日存档」手动按钮

### 市场热力图
- 个股热力图（按板块分组 treemap，面积≈市值，色=当日涨跌）
- 板块热力图（市值加权）；板块可自定义编辑

### 部署 / 基础设施
- Docker 化，仅绑回环 `127.0.0.1:8799`；Cloudflare Tunnel 出站 + Access 零信任登录
- SQLite（WAL）持久化，K 线缓存跨重启；收盘后后台刷新重点标的
- 盘后总结 / 告警经 tv-relay 转 Telegram
